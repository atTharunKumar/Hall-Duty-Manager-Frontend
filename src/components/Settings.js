import React from 'react';
import './Settings.css';  // Import the CSS file

const Settings = () => {
  return (
    <div className="settings">
      <h2>Settings</h2>
      <p>This feature is under development. Here you can configure system settings.</p>
    </div>
  );
};

export default Settings;
